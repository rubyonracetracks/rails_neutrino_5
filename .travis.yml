services:
  - docker

before_script:
  - sudo apt-get install -y bash
  - gem uninstall -ix /home/travis/.rvm/gems/ruby-2.5.3@global bundler

script:
  - $PWD/build-rails 'tmp1'

after_script:
  - cd $PWD/tmp1 && bundle install && rails db:migrate && rails test
